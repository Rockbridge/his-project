spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/his_db
    username: his_user
    password: his_password
  flyway:
    enabled: true
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
